# Implementing a simple book regiser using MEAN stack.
# update 
sudo apt update
# upgrade 
sudo apt upgrade -y
# add certificate
sudo apt -y install curl dirmngr apt-transport-https lsb-release ca-certificates
# check for the latest version 
curl -sL https://deb.nodesource.com/setup_22.x | sudo -E bash -
# Install node JS
sudo apt install nodejs -y
node -v
# install npm
sudo apt update
sudo apt install npm
sudo apt update
sudo npm install body-parser
# install mongoDB. install the repository keys first
sudo apt-get install -y gnupg curl
curl -fsSL https://www.mongodb.org/static/pgp/server-7.0.asc | \
   sudo gpg -o /usr/share/keyrings/mongodb-server-7.0.gpg \
   --dearmor
 # add the mongodb repository package to the ubuntu package list
 echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-7.0.gpg ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list
 sudo apt update
 # add mongodb packages
sudo apt install gnupg curl
sudo apt update
sudo apt install mongodb-org -y
mongod --version
sudo systemctl status mongod
sudo systemctl start mongod

# create books folder and initialize npm
mkdir Books && cd Books
init npm
cat package.json
vi server.js:

cat server.js
# install express and setup server route
npm install express mongoose
# create an app folder
mkdir apps && cd apps
vi routes.js:

cat routes.js
# create a new foder: models
mkdir models && cd models
vi book.js:

cat book.js
cd ../..
mkdir public && cd public
vi scripts.js:

cat scripts.js
vi index.js:

cat index.html:

cat index.html
cd ..
node server.js
# goto aws instance and add new inbound security set the port to 3300
# connect to instance using pulic ip:3300